let InitialState = {}
export function ClassName(state = initialState, action) {
    switch (action.type) {
        case 'DUMMY_ACTION_TYPE': {

        }
        default:
            return state
    }
}